# fly.toml do liongo-calls (deploy LionChat do WaCalls)
# Base: upstream JotaDev66/WaCalls (develop, pure-Go) + delta LionChat (auth/ICE/healthz).
# O health check do Fly usa /healthz (rota nossa). O Dockerfile é o oficial do upstream.
app = "liongo-calls"
primary_region = "gru"

[build]
  dockerfile = "Dockerfile"

[[mounts]]
  source = "wacalls_data"
  destination = "/data"

[http_service]
  internal_port = 8080
  force_https = true
  auto_stop_machines = false
  auto_start_machines = false
  min_machines_running = 1

  [http_service.concurrency]
    type = "connections"
    hard_limit = 200
    soft_limit = 150

  [[http_service.checks]]
    interval = "15s"
    timeout = "3s"
    grace_period = "10s"
    method = "GET"
    path = "/healthz"

# CPU DEDICADA (2026-08-22): mídia em tempo real (WebRTC + PCM + codec WhatsApp)
# sofria jitter na shared-cpu-1x (burstável/throttle) sob ligações simultâneas.
# performance-1x = 1 vCPU dedicado + 2GB. NÃO voltar pra shared sem medir o pico.
[[vm]]
  size = "performance-1x"
  memory = "2048mb"
